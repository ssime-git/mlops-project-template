# Drift Detection Configuration
defaults:
  - _self_

data:
  reference_path: data/raw/reference.csv
  current_path: data/raw/current.csv

drift:
  output_path: reports/drift/drift_report.html
  threshold: 0.5  # Drift detection threshold

evidently:
  metrics:
    - data_dift
    - target_drift