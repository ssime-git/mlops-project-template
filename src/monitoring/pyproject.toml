[project]
name = "monitoring"
version = "0.1.0"
description = "Monitoring and drift detection"
requires-python = ">=3.10"
dependencies = [
    "prometheus-client>=0.19.0",
    "evidently>=0.4.0",
    "common",
]